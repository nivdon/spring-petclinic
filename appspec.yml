version: 0.0
os: linux
files:
  - source: target/
    destination: /home/ec2-user/student/
hooks:
  AfterInstall:
   - location: setup.sh
     timeout: 300
     runas: root
  ApplicationStart:
   - location: run.sh
     timeout: 300
     runas: root
